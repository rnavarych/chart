import {StyleSheet} from 'react-native';

const styles = StyleSheet.create({
  container: {
    height: '100%',
    backgroundColor: '#fac',
  },
  chartContainer: {
    padding: 20,
  },
});

export default styles;
