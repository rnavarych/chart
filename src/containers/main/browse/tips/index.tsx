import React from 'react';
import { View } from 'react-native';
import styles from './styles';

interface Props {
  navigation: any;
}

function TipsScreen(props: Props) {
  const { navigation } = props;
  return <View />;
}

export default TipsScreen;
